## Settings file was created by plugin BetterSleepBruh v2.0.10
## Plugin GUID: vapok.mods.BetterSleepBruh

[Local Config]

## If enabled, displays the mod overview and links splash screen on game startup. [Not Synced with Server]
# Setting type: Boolean
# Default value: true
Show Splash on Startup = false

## If enabled, sends anonymous mod launch and heartbeat telemetry to help improve mod stability and track active versions. [Not Synced with Server]
# Setting type: Boolean
# Default value: true
Enable Anonymous Telemetry = false

[Log Output Configuration]

## Toggles Log Output
# Setting type: Boolean
# Default value: true
Logging Enabled = true

## Minimum Log Level to Output
# Setting type: LogLevels
# Default value: Warning
# Acceptable values: Debug, Info, Warning, Error, Fatal
Log Level = Warning

[Server Settings]

## Default is false/disabled; Set to True/Enabled to resume Vanilla Sleep Start [Synced with Server]
# Setting type: Boolean
# Default value: false
Use Vanilla Sleep Start = true

## Day Fraction to allow sleep to begin. Default is 0.5, or Noon. Only applies when Vanilla Sleep Start is disabled/false. [Synced with Server]
# Setting type: Single
# Default value: 0.5
# Acceptable value range: From 0 to 0.99
Sleep Start = 0.5

## Scales the bonus increment (added on top of normal time): extra rate = this × sleep fraction × 10. At 1.0 and all-but-one in bed, extra rate is 10 (time advances 11× vs vanilla dt alone). [Synced with Server]
# Setting type: Single
# Default value: 0.6
# Acceptable value range: From 0 to 1
Bonus Multiplier = 0.6

## Scales the bonus increment (added on top of normal time): extra rate = this × sleep fraction × 10. At 1.0 and all-but-one in bed, extra rate is 10 (time advances 11× vs vanilla dt alone). [Synced with Server]
# Setting type: Single
# Default value: 20
# Acceptable value range: From 0 to 30
Bonus Increment Scale = 20

## Partial boost linearly ramps to zero over this many real-time seconds before the next morning. Uses net rate (1 + extra) so higher boost = longer game-time taper. 0 = no taper (hard cut only at morning). [Synced with Server]
# Setting type: Single
# Default value: 3
# Acceptable value range: From 0 to 30
Boost Fade (Real Seconds) = 3

[Testing Mode]

## When enabled, Fake Total Players and Simulate Players In Bed are added on top of real player counts for boost math and HUD (server + RPC). [Synced with Server]
# Setting type: Boolean
# Default value: false
Enable Testing Mode = false

## Count of fake connected players to add to the server while Testing Mode is on. [Synced with Server]
# Setting type: Int32
# Default value: 10
# Acceptable value range: From 0 to 80
Fake Total Players = 10

## Count of fake players in bed to add while Testing Mode is on (clamped to Fake Total Players). [Synced with Server]
# Setting type: Int32
# Default value: 1
# Acceptable value range: From 0 to 80
Simulate Players In Bed = 1

